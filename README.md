<img width="1000" alt="p1" src="https://github.com/akkva/gwfox/assets/150437642/930d47f1-0700-4a02-809b-46890cdeec60">

<div align="center">
  
本主题适用mac&win

</div>

## 使用方法
1. 把下载的 `chrome` 文件夹放入火狐 `配置文件夹` 中
2. 在 `about:config` 页面中搜索 `toolkit.legacyUserProfileCustomizations.stylesheets`  `svg.context-properties.content.enabled` 并设为 `true`
3. 重启火狐

## 注意事项
工具栏按钮数量仅支持默认数量（左3右4）

## 文字提示

win下全局 `文字提示` 定制需要[userChrome.js](https://github.com/MrOtherGuy/fx-autoconfig)加载

如有需要把 `tooltip` 文件夹里的内容合并到对应的目录

`profile` →  `配置目录`  `program` →  `程序目录`

## 样式调整

书签工具栏从底部弹出：在 `about:config` 页面添加 `uc.tweak.popout_bookmarks_toolbar` 并设为 `true`

弹出菜单/面板背景模糊(macOS)：在 `about:config` 页面添加 `uc.tweak.blur` 并设为 `true` 

<img width="1000" alt="p2" src="https://github.com/akkva/gwfox/assets/150437642/7f37382d-2f2a-4f73-a78d-037e8ceb58cc">
